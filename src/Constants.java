import org.apache.hadoop.io.NullWritable;

/**
 * Constants used across the project
 */
public class Constants {

    public static final String WORD_SEPARATOR = "-:-";

    public static final NullWritable NULL_WRITABLE = NullWritable.get();
}
